# Python
__pycache__/
*.pyc
.venv/
venv/
*.log

# Node / frontend build output
frontend/node_modules/
frontend/dist/
frontend/dist-ssr/
frontend/*.local

# Env files (never commit secrets/local overrides)
.env
.env.local
*/.env
*/.env.local

# Large local-only assets not needed for deployment
Stockfish/

# Old local backups (kept on disk, not tracked in git)
frontend_working_backup/
frontend_before_captured_pieces/
